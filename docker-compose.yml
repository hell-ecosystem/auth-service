services:
  redis:
    image: redis:7.2
    container_name: auth_redis
    ports:
      - "6379:6379"
    restart: always
    volumes:
      - redis-data:/data
    command: ["redis-server", "--save", "60", "1", "--loglevel", "warning"]
    
volumes:
  redis-data:
